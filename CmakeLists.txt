cmake_minimum_required(VERSION 3.10)
project(pointperfect_ntrip_client LANGUAGES CXX)

set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

add_executable(pointperfect_ntrip_client pointperfect_ntrip_client.cpp)

# POSIX libraries
target_link_libraries(pointperfect_ntrip_client PRIVATE pthread)

# If building on some systems you might need -lm (math) — CMake usually adds it automatically.
